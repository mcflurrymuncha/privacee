name: build and upload privacee artifacts

on:
  push:
    branches: [main]
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [windows-latest, macos-latest, ubuntu-latest]

    steps:
      - name: check out repository
        uses: actions/checkout@v4

      - name: set up node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: install dependencies
        run: npm ci

      - name: compile typescript
        run: npm run build

      # build local directory binaries using electron-builder
      - name: package app binaries
        run: npx electron-builder --publish never
        env:
          github_token: ${{ secrets.GITHUB_TOKEN }}

      # upload the generated installer assets to the actions summary panel
      - name: upload production build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: privacee-${{ matrix.os }}-build
          path: |
            out/*.exe
            out/*.dmg
            out/*.deb
          if-no-files-found: error
          retention-days: 14
